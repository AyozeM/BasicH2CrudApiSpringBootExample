# Api rest con CRUD hecha en Spring Boot
Ejemplo de api rest Spring boot con base de datos h2 en memoria

Para usarlo descarga el código y y ejecuta en la raiz el comando
```bash
mvn spring-boot:run
```